{% extends 'base.html.twig' %}

{% block title %}{{ 'usun_zdjecie'|trans({'%id%': photo.id}) }}{% endblock %}

{% block body %}

    <div class="col-md-3">
        <a href="{{ global.request.headers.get('referer') }}" title="{{ 'label.back'|trans }}" class="btn btn-default">
            <span class="fa fa-mail-reply" aria-hidden="true"></span>
            <i class="glyphicon glyphicon-arrow-left"></i>
            {{ 'label.back'|trans }}
        </a>
    </div>

    <h1>{{ 'usun_zdjecie'|trans({'%id%': photo.id}) }}</h1>
    {{ 'czy_na_pewno_chcesz_usunac_to_zdjecie'|trans}}
    {{ form_start(form, { method: 'post', action: url('photo_delete', {'id': photo.id}) }) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ 'action.delete'|trans }}" class="btn btn-danger" />
    {{ form_end(form) }}


{% endblock %}