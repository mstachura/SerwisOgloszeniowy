


    <h1>Dodawanie komentarza{{ 'title.tag_add'|trans }}</h1>

    {{ form_start(form, { method: 'post', action: url('ads_view', {'id': advertisement.id}) }) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-success" />
    {{ form_end(form) }}
