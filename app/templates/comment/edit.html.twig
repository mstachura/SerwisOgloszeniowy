{% extends 'base.html.twig' %}

{% block title %}{{ 'edytuj_komentarz'|trans({'%id%': comment.id}) }}{% endblock %}

{% block body %}
    <h1>{{ 'edytuj_komentarz'|trans({'%id%': comment.id}) }}</h1>
    {{ form_start(form, { method: 'post', action: url('comment_edit', {'id': comment.id}) }) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-success" />
    {{ form_end(form) }}
{% endblock %}