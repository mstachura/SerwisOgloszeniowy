{% extends 'base.html.twig' %}

{% block title %}{{ 'wyszukiwarka'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'ogłoszenia_kategorii'|trans }} {{ name_category }}</h1>


    {% for advertisement in advertisements %}
        <a href ="{{ url ('ads_view', {'id': advertisement.id}) }}">
            {{ advertisement.name }}
        </a>
    {% endfor %}

{% endblock %}